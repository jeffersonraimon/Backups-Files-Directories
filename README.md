# Backups-Files-Directories
This Python script creates a compressed backup of important server configuration files and directories, uploads the backup to an FTP server, and then cleans up the temporary file.
